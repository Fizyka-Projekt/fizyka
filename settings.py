radius = 1
max_v = 20
mass = 1
n_H = 20
n_L = 20
height = n_H * radius
width = n_L * radius
max_atoms_number = 50 # int(0.25 * min([n_H, n_L]))
atoms_number = 12
k = min([n_H, n_L])
dt = 1 / (k * max_v)  # krok czasu
frames = 150  # ilość odświeżeń
Dt = frames * dt  # czas eksperymentu
d = radius / 10  # tolerancja zderzenia
radius_plus = radius + d  # radius with toleration
